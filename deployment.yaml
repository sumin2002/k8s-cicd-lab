apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-chatbot
  template:
    metadata:
      labels:
        app: my-chatbot
    spec:
      imagePullSecrets:       # 👈 [추가] Harbor 열쇠 사용 선언
      - name: regcred
      containers:
      - name: my-web
        # 👇 [수정] 주소 앞에 Harbor IP 붙이기
        image: 192.168.1.19/library/my-web-app:1f70b7de87d435b6ad78c1438418b6d2cbfa49e2
        ports:
        - containerPort: 80
