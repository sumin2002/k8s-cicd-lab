apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-chatbot
  template:
    metadata:
      labels:
        app: my-chatbot
    spec:
      imagePullSecrets:       # 👈 [추가] Harbor 열쇠 사용 선언
      - name: regcred
      containers:
      - name: my-web
        # 👇 [수정] 주소 앞에 Harbor IP 붙이기
        image: 192.168.10.20/library/my-web-app:f42d85d48680555e466ad194ad756f130801ac70
        ports:
        - containerPort: 80
